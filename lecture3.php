<?php>
if (is_string('hello'))
 echo "It is a string";
 else 
     echo 'It is not a string';
 var_dump(is_string('XYZ'));
 var_dump(is_string('8008'));
 var_dump(is_string('123'));
 var_dump(is_string('false'));
 
 
 
 if (is_string('bye'))
 echo "string";
 else 
 echo 'not a string';
  var_dump(is_string('123'));
  var_dump(is_string(is_string('false'));
  
  if (is_string('car'))
  echo 'It is a string';
  else 
  echo 'It is not a string';
  var_dump(is_string('XYZ'));
 var_dump(is_string('8008'));
 var_dump(is_string('123'));
 var_dump(is_string('false'));
 ?>
  
  
  